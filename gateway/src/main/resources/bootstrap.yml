server:
  port: 10003

spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:1501
        file-extension: yml
      discovery:
        server-addr: 127.0.0.1:1501
    gateway:
      enabled: true
  redis:
    database: 0
    host: 192.168.15.35
    port: 6379
    password:
    timeout: 5000
    lettuce:
      pool:
        max-active: 100
        max-idle: 50
        max-wait: -1
        min-idle: 10
