server:
  port: 10003

spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:1501
        file-extension: yml
      discovery:
        server-addr: 127.0.0.1:1501
    gateway:
      enabled: true
